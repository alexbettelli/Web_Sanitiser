use sanitiser_core::config::Policy;

fn main() -> anyhow::Result<()> {
    // Stub: qui andranno parsing argv, lettura file/dir/URL, chiamata a sanitiser_core
    let _policy = Policy::default();
    println!("web-sanitiser CLI — scheletro iniziale");
    Ok(())
}